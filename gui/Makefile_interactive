# ============================================================================
# Makefile pour JARVIS Interactive - Interface Améliorée
# ============================================================================

CXX = g++
CXXFLAGS = -std=c++17 -Wall -Wextra -pthread
INCLUDE = -I./include
TARGET = jarvis_interactive
SRC_DIR = src
OBJ_DIR = build
SOURCES = $(SRC_DIR)/main_interactive.cpp
OBJECTS = $(OBJ_DIR)/main_interactive.o

# Cibles par défaut
.PHONY: all run debug clean help

all: $(TARGET)
	@echo "✓ Exécutable créé: $(TARGET)"
	@echo "✓ Compilation réussie! Exécutez avec: make run"

$(TARGET): $(OBJECTS)
	@mkdir -p $(OBJ_DIR)
	$(CXX) $(CXXFLAGS) $(INCLUDE) -o $@ $^

$(OBJ_DIR)/%.o: $(SRC_DIR)/%.cpp
	@mkdir -p $(OBJ_DIR)
	$(CXX) $(CXXFLAGS) $(INCLUDE) -c $< -o $@

run: $(TARGET)
	./$(TARGET)

debug: CXXFLAGS += -g -O0
debug: clean $(TARGET)
	gdb ./$(TARGET)

clean:
	@rm -rf $(OBJ_DIR) $(TARGET)
	@echo "✓ Nettoyage effectué"

help:
	@echo "Cibles disponibles:"
	@echo "  make all       - Compiler"
	@echo "  make run       - Compiler et exécuter"
	@echo "  make debug     - Compiler en mode debug"
	@echo "  make clean     - Nettoyer les fichiers compilés"
	@echo "  make help      - Afficher cette aide"
